# CLAUDE_MEMO.md

複数セッションにまたがってClaude Codeが行った調査・意思決定とその理由を記録する
作業ログ。`.py`ファイル冒頭の変更履歴コメントは「何を変えたか」の記録だが、この
ファイルは「なぜそう判断したか」「何を確認済みで何が未確認か」まで含めて記録する。

新しい調査・変更を行ったら、このファイルの一番上(このすぐ下)に日付付きで追記する
こと(`.py`の変更履歴コメントと同じく新しい方が上)。

参照ルールは [CLAUDE.md](CLAUDE.md) の「Project policy」節を参照。プログラムの変更や
過去の経緯確認を行う際は、`CLAUDE.md`・`進捗反映版メモ.txt`・このファイルの3つを
必ず参照すること。

---

### 2026-08-15: L字マップ(map_configs/l_map.json)を第二検証環境として試すも不採用

- **背景**: ユーザーから「これまでの検証はサンプル数(独立した実歩行データ)が少なすぎ
  るのでは」との指摘。確認したところ、`map_configs/kanri_4f.json`の比較実験が対象と
  している実測CSVは`exclude_csv`で4本を除外した結果`pdr_log_0805_1438/1441/1442.csv`
  の**3本のみ**だった(`--seeds`で振っていた複数シードはPF内部の乱数のばらつきを
  見ているだけで、独立した歩行サンプルを増やすものではないことを再確認)。
- **試みた対応**: `Cross.py`/`L_map.png`と同様に昔から存在する`test7.py`用の合成
  L字地図(`Lmap.py`が生成する`L_map.png`)を使い、kanri_4fで除外されている4CSV
  (`pdr_log_0410_1535/0414_0902/0414_0940/0624_14.csv`)を`pdr_pf_improved.py`の
  新機能(route_source=auto等)に通す`map_configs/l_map.json`を新規作成した。
  scale_px_per_m=40.0・target_distance_px=700.0・step_gain=1.12は`test7.py`の既存
  キャリブレーション定数をそのまま流用。route_pointsはLmap.pyの矩形描画座標から
  幾何学的に算出([[50,250],[370,250],[370,630]])。CSVヘッダーを確認したところ
  この4CSVには`yaw_deg`列がなく(0805系3CSVにはある)、`--heading-source gyro`を
  明示指定する必要があることが分かった。
- **技術的には成功**: パイプラインはクラッシュせず完走し、`extract_auto_route_mask()`
  はL字通路の形状を正確に抽出した(結果画像: `results/20260815_230510_route-enforce-
  auto_seed-42.png`)。
- **しかし不採用と判断した理由**: `none`条件と`auto-enforce`条件の診断値(全滅回数・
  有効粒子率・Neff・最終位置)が完全に一致した。原因を調べると、`L_map.png`は
  `Lmap.py`が通路の矩形2つだけを白(移動可能)として描いた合成画像で、**周囲に部屋や
  開けた空間が一切ない**(前処理済み地図のpassage_ratio=0.078と、自動抽出した通路
  マスクの有効画素比率0.078が完全一致)。つまりこの地図では「移動可能領域」＝
  「通路」であり、route_source=autoが検証したい「部屋(広い領域)と通路(狭い領域)の
  区別」という効果自体を原理的に測定できない。地図を改修して部屋を追加する案も
  検討したが、実測ではなく完全に架空の地図になり「独立した2つの環境で検証した」
  という主張の説得力が弱いため見送った。
- **ユーザーからの補足**: L_map/test7.pyはそもそも研究初期に「PDR軌跡が地図上に
  正しく表示されるか」を確認するための技術検証という側面が大きかった、とのこと。
  また、kanri_4Fの歩行データは学校施設のため追加収集が比較的容易とのこと。
- **結論・方針**: `map_configs/l_map.json`と関連ファイルは(ユーザーの意向により)
  削除・移動せずそのまま残すが、卒論本文の比較実験・route_source=auto検証には
  組み込まない。サンプル数不足への対応は、L字地図での代替検証ではなく
  **kanri_4Fでの実歩行データの追加収集**を優先する方針とした(8.8節参照)。
  また、L_mapが担っていた「地図座標系・スケール較正の妥当性確認」という役割は、
  卒論では3.4節(建物地図: 二値化・縮尺px/mの求め方)と4.8節(従来方式の予備実験・
  失敗例。実データで発見した「y≈300張り付きバグ」「prefer時の部屋漏れバグ」を
  失敗例として記載できる)に実データベースで既に自然に組み込まれているため、
  L_mapへの言及なしで開発過程を説明できると整理した。
- **compare_route_source.pyの副次的な改善**: この調査に伴い、`EXPECTED_ENDPOINT_X`を
  map_config別の既定値辞書(`DEFAULT_EXPECTED_ENDPOINT_X`)+`--expected-endpoint-x`
  CLI引数に変更した。既知でないmap_config(l_map.json等)では終点x誤差の算出を自動的
  にスキップするようにした。これはkanri_4f.jsonの既存動作には影響しない汎用的な
  改善であり、そのまま残す。

### 2026-08-15: 不要ファイルの調査・「削除候補」フォルダへの移動

- **経緯**: ユーザーから「i22satou内には卒業研究においてもう不要なファイルが多い
  はず。見つけて『削除候補』フォルダへ移動してほしい。resultsは重点的に確認して」
  との依頼。削除ではなく移動(元に戻せる)なので、判断に迷うものは移動しつつ
  ここに理由を明記する方針で対応した。
- **内容を実際に開いて確認したもの(誤って移動しないため)**:
  - `L.png`: 開くとtest7.pyが生成した、4つの除外CSV(0410/0414×2/0624)の合成L字
    地図上の軌跡だった。以前ユーザーが「別のL.pngで使用中」と言っていたファイル
    そのものと確認できたため**残した**。これを生成した`test7.py`と、その入力
    (`Lmap.py`が生成する`L_map.png`)も再現性のため一緒に残した(他のtest系
    プロトタイプとは扱いを分けている)。
  - `kanri_4f_preview_final3.png`: 元の平面図→検出壁(赤/緑)→二値化、の3段階を
    示す図。進捗メモの卒論章構成「3.4 建物地図」にそのまま使える資料と判断し
    残した。
  - `result_none.png`/`result_prefer.png`: 開くと、今回のセッションで修正した
    「y≈300に張り付く」バグと「prefer時に部屋へ漏れる」バグの、修正前の
    スクリーンショットだった。技術的には古い結果なので移動したが、進捗メモの
    卒論章構成「7.7 失敗例」(誤った曲がりの例・部屋へ誤進入した例)にそのまま
    使える可能性があるため、ユーザーへの報告で個別に言及した。
- **移動したもの(ルート直下)**: `test3.py`〜`test6.py`, `test8.py`〜
  `test11_gemini.py`(test7.pyを除く historical prototypes)、
  `adaptive_behavior_particle_filter_pdr_route_fixed.py`、`pdr_particle_filter.py`
  (どこからも参照されていない孤立プロトタイプ、grep で確認済み)、`Cross.py`・
  `cross_map.png`(合成十字路マップ。L_map.pngと違い、現行のどの成果物にも
  紐付いていないことを確認)、`test10_result.png`・`test11_gemini_result.png`・
  `output_fixed.png`・`output_fixed2.png`・`result_none.png`・`result_prefer.png`
  (いずれも本セッションで修正済みのバグ入り、または現行パイプライン以前の
  旧世代の出力画像)。
- **移動したもの(results/、69個中63個)**: `compare_route_source.py`の複数シード
  ×複数条件の比較実験(本セッション中に何度も実行)で自動保存された、ほぼ同一の
  PNGの山。研究的価値は個々のPNGではなくCSVサマリ(比較表)にあるため、最新の
  4条件比較の1シード分(seed=42)のPNG4枚と、CSVサマリ2つ(4条件版の最新版
  `20260815_223555_route_source_comparison.csv`、センサー品質診断
  `20260815_220908_sensor_quality.csv`)だけをresults/に残し、残り63個
  (dilation実験・センサー品質検証以前の中間結果・3条件版の古いCSV等)を移動した。
- **確認方法**: 各画像は実際に`Read`で開いて内容を目視確認してから判断した
  (ファイル名やタイムスタンプだけで判断していない)。どのスクリプトがどの画像
  ファイルを入力として参照しているかは`grep`で確認した(例: `cross_map.png`/
  `L_map.png`を参照するスクリプト一覧を洗い出し、`L.png`だけが現行の
  `exclude_csv`運用と直結していることを特定)。
- **削除は一切行っていない**。全て`削除候補/`(と`削除候補/results/`)への移動の
  みで、最終的に削除するかどうかはユーザーの判断に委ねている。CLAUDE.mdの
  「Script lineage」節にも移動の経緯を記載した。

### 2026-08-15: 不確実性適応粒子数(§6.5)を実装・検証 — 全滅回数は明確改善、精度は小幅改善

- **背景**: ユーザーから「一旦検証(1441/1442の謎)はここまでにして§6.5不確実性適応
  粒子数へ切り替えたい」との指示。route_maskの幅・センサー品質・乱数シードいずれ
  でも1441/1442差の真因を特定できなかったため、いったんその調査は保留し、進捗メモ
  §6.5(独立した価値のある機能、Neffが計算済みで未使用という理由で最優先候補と
  位置づけられていた)の実装に着手した。
- **実装**: `ParticleFilterPDR.configure_behavior()`で、移動様態ベースの粒子数
  (先行研究の枠組み)に加えて、直前ステップの`neff_ratio = 直前Neff / 直前粒子数`
  を見て、閾値未満(既定0.30)なら×1.5、閾値超(既定0.60)なら×0.75、を掛けて
  [80, 1200]にクリップする処理を追加。既定は無効(`UNCERTAINTY_ADAPTIVE_PARTICLES
  =False`)で、`--uncertainty-adaptive-particles`/JSON `adaptive_pf.
  uncertainty_adaptive_particles`で明示的に有効化しない限り既存挙動を維持する
  (無効時の出力が変更前と完全一致することを確認済み)。
- **検証**: `compare_route_source.py`に4つ目の条件`auto-enforce-unc`(auto-enforce
  +不確実性適応)を追加し、6シード(1, 7, 42, 100, 777, 2024)で
  auto-enforce(既存)と比較した。
  - **全滅回数**: シードごとの3ファイル合計が47→45, 47→44, 47→42, 48→44, 46→44,
    45→44と、**6シード全てで例外なく減少**した(平均15.6回→14.6回)。特に
    pdr_log_0805_1442.csvは全滅回数が6シード中4シードで3→2に改善し、悪化した
    シードは一つもなかった。
  - **終点x誤差**: 平均では339.9px→329.7pxと改善したが、ファイル別・シード別に
    見ると一様ではない。pdr_log_0805_1438/1442.csvは概ね改善傾向だが、
    pdr_log_0805_1441.csvは6シード中2つ(seed7: 341→494px、seed2024: 398→506px)
    で明確に悪化しており、全体としては「改善する場合が多いが保証はできない」
    という結果になった。
  - **結論**: 「不確実性が高い時に粒子を増やす」ことで、全滅(完全な見失い)という
    最も分かりやすい不安定性の指標は確実に改善できることが分かった。一方、
    それが最終的な位置精度の大幅な改善に直結するとまでは言えない。控えめだが
    本物の(6シード全てで同方向の)改善効果、という正直な結論。1441/1442の未解明の
    性能差(前項参照)を解消する決定打にはならなかった。
- **結果**: `results/20260815_223555_route_source_comparison.csv`。
- **今後**: 閾値(0.30/0.60)・倍率(1.5/0.75)は初期値のままでチューニングしていない
  ため、感度分析(パラメータを振って効果の変化を見る)が次の候補。また、
  position_spread_history(位置分散)は今回は制御に使わず診断用のまま残しており、
  Neff比率と組み合わせて使うかどうかも今後の検討課題。

### 2026-08-15: compare_route_source.pyに--seeds追加 — 複数シードで再検証

- **目的**: 直前の項目で、1441と1442は`np.random.seed(42)`実行1回内で同じ乱数
  ストリームを順番に消費しているため、「1441の方が悪い」という結果が本質的な差
  ではなく乱数の偏りに過ぎない可能性を排除できていなかった。これを切り分けるため
  `compare_route_source.py`に`--seeds`(複数指定、例: `--seeds 1 7 42 100 777 2024`)
  を追加し、seed列を結果CSVに含め、auto-enforce条件についてファイル別・シード別の
  内訳を表示する機能を追加した。
- **結果**: 6シード(1, 7, 42, 100, 777, 2024)全てで、auto-enforce条件の全滅回数は
  1438が23〜24回、1441が19〜21回、1442が2〜3回と、**どのシードでも同じ順位・
  ほぼ同じ値に収まった**(終点x誤差も1442が59〜70pxに対し1441は341〜524pxと、
  差が常に大きい)。**1441と1442の性能差は乱数の偏りによるものではなく、再現性の
  ある実在の現象であることが確定した。** 原因(センサー品質診断でも決定的な差を
  示せていない)は依然として未特定のまま。
- **副産物の発見**: none条件(経路制約なし)での1442.csvの終点x誤差が、シードに
  よって1.1px〜315.2pxまで激しくばらついた(同じCSV・同じアルゴリズムでも、経路
  制約が無いとPFの結果自体が乱数(粒子初期配置等)に大きく左右される)。一方、
  同じ1442.csvがauto-enforce条件では6シード全てで59〜70pxに収まっており、
  **経路制約(特にauto)は平均精度だけでなく結果の再現性(乱数依存性の低さ)も
  大きく改善する**という、クリーンで説得力のある効果を確認できた。8.1節
  (位置精度への効果)にそのまま使える。
- **今後**: 1441/1442の性能差の真因はまだ特定できていない(route_maskの幅・
  乱数・センサー品質(方位系)のいずれでも説明できず)。加速度側(歩幅推定)の
  品質診断が次の候補として残っている。ただし優先度としては、この謎の解明に
  これ以上時間をかけるより、§6.5(不確実性適応粒子数)など独立した価値のある
  機能に進む方が生産的というのがここまでの調査を踏まえた判断で、ユーザーに
  報告のうえ相談する。

### 2026-08-15: check_sensor_quality.py — CSVごとのセンサー品質診断(結果は予想と逆)

- **目的**: 直前の項目で「pdr_log_0805_1441/1442.csvは開始位置・移動様態判定傾向が
  ほぼ同じなのに全滅回数が20回 vs 3回と大きく違う→route_maskではなくCSVごとの生
  センサーデータの質の差が原因では」という仮説を立てた。これを検証するため、
  `pdr_pf_improved.py`本体には手を加えず、同ファイルの`get_yaw_rate()`/
  `apply_map_config()`/`validate_log()`を再利用する読み取り専用の診断スクリプト
  `check_sensor_quality.py`を新規作成した。
- **計測した指標**: (1)ヨーレート(ジャイロを重力方向へ投影した値、移動様態判定に
  実際に使われている量そのもの)の標準偏差・最大絶対値・turn_yaw_rate_threshold
  (20deg/s)を超える割合(曲がり率)、(2)加速度合成値の標準偏差(手ぶれ指標)、
  (3)yaw_deg(Android融合方位、heading-source=androidで実際に使われる値そのもの)の
  サンプル間差分の標準偏差・最大値・記録全体の正味変化量。
- **結果(`results/20260815_220908_sensor_quality.csv`)**: 予想に反して、最も
  性能が悪かった1441.csvは**yaw_degの飛び(ジャンプ)が3ファイル中最も小さく**
  (差分標準偏差0.73°、瞬間最大ジャンプ7.6°)、最も性能が良かった1442.csvは
  **瞬間最大151.3°という明らかなセンサーグリッチ(1サンプルでの物理的にありえない
  跳躍、おそらく回転ベクトルセンサの再フュージョン)を含む**にもかかわらず良好な
  結果だった。ヨーレート標準偏差・加速度分散も3ファイルでほぼ同水準で、明確な
  差は無かった。唯一、予想通りの方向だったのは「曲がり率」(ヨーレートが閾値を
  超える割合)で、1442=0.219が最も低く1438/1441=0.255〜0.258よりやや低かったが、
  差は14%程度に留まり、全滅回数20 vs 3という差を単独で説明できるほど劇的ではない。
- **結論**: 「CSVごとのセンサー品質(ノイズ・ジャンプ)の違い」という仮説は、
  少なくとも今回計測した指標では明確に支持されなかった。曲がり率の差は方向性
  としては手がかりになりうるが、決定的ではない。
- **新たに見つけた見落としていた変数**: `main()`で`np.random.seed(args.seed)`が
  実行全体で一度だけ呼ばれ、複数CSVは同じ乱数ストリームを順番(ファイル名の
  アルファベット順、1438→1441→1442)に消費している。つまり1441と1442は厳密には
  「同一の乱数条件」で比較できておらず、それぞれ異なる区間の乱数列を引いている。
  1441と1442の結果の差が本質的な要因によるものか、単に引いた乱数列の偏りによる
  ものかを、この時点では切り分けられていない。
- **今後**: 複数の乱数シードで同じ比較(compare_route_source.py相当)を繰り返し、
  1441が1442より一貫して悪い結果になるか、それとも順位がシードごとに入れ替わるか
  を確認するのが、次の安価な切り分けとして妥当。それでも差が残るなら、センサー
  品質以外の要因(加速度側の歩幅推定誤差、ノイズが発生するタイミングとマスク境界
  との位置関係など)をさらに調べる必要がある。ユーザーに報告のうえ相談する。

### 2026-08-15: auto_route_dilation_pxを追加して検証 → 仮説棄却、真因は別にある可能性

- **経緯**: 直前の項目(compare_route_source.py)で「auto-enforceがmanual-enforceに
  平均で劣る原因は、自動マスクが実際の廊下幅(約22〜24px)に忠実なのに対し手動の
  一様バッファ(半径18px→幅37px)が実際より広い余裕を粒子に与えていたから」という
  仮説を立てた。この仮説が正しければ、自動マスクへ数px程度の緩和的膨張を加えれば
  1438/1441.csvの全滅回数がmanual-enforce相当まで下がるはずである。
- **実装**: `extract_auto_route_mask()`に`dilation_px`引数を追加し、最大連結成分を
  採用した後に指定半径で`binary_dilation`する処理を追加(地図由来の形状はそのまま
  維持し、境界にのみ余裕を追加)。CLI `--auto-route-dilation-px`、JSON任意設定
  `auto_route_dilation_px`(共に既定0px=従来通り)として公開。
- **検証結果(仮説棄却)**: 膨張半径を0/5/8/10pxで振って`pdr_log_0805_1438/1441.csv`
  の全滅回数を比較したところ、**ほぼ変化しなかった**(1438: 24→24→24→24回、
  1441: 20→20→21→19回、いずれも誤差の範囲)。10px膨張(マスク幅がmanual相当の
  37px近くまで拡大)しても改善しないため、「マスク幅の不足」という当初の仮説は
  データによって否定された。
- **追加調査**: `pdr_log_0805_1441.csv`と`pdr_log_0805_1442.csv`は登録済み開始位置
  が完全に同一(x=146.6, y=230.8)、移動様態の内訳(直進/曲がりの比率)もほぼ同じ
  (曲がり判定が全体の80〜89%を占める点も共通)であるにもかかわらず、全滅回数は
  20回 vs 3回、到達距離も大きく異なる(x=294 vs x=741)。マスク形状・開始位置・
  移動様態判定の傾向がほぼ同じ2ファイルでここまで結果が違うことから、**route_mask
  の形状ではなく、CSVごとの生センサーデータそのものの質(ジャイロノイズ・ドリフト
  の大きさ、手ぶれの程度など、計測セッションごとに異なる特性)が支配的な要因である
  可能性が高い**と判断した。
- **結論・今後**: `auto_route_dilation_px`は今後のチューニング用に機能として残すが
  (既定0pxなので現状の挙動には影響しない)、この問題の解決に単独では効かないことが
  分かったため、これ以上マスク形状を追う優先度は下げる。代わりに、(1)センサーデータ
  の質のばらつき自体を定量化する(CSVごとのジャイロ角速度の分散・ドリフト量を比較する
  診断を追加する等)、(2)進捗メモ§6.5(不確実性に応じた動的粒子数変更、実装コストが
  低く優先実装候補とされている)に進む、のいずれかが次の一手として妥当と判断し、
  ユーザーに報告のうえ相談する。

### 2026-08-15: compare_route_source.py — none/manual-enforce/auto-enforceの定量比較

- **目的**: 前項(route_source=auto)を実装した直後、ユーザーから「③(評価基盤)に着手してよい」
  との承認を受けて実施。`pdr_pf_improved.py`本体には手を加えず、CLI経由で3条件
  (none / manual-enforce / auto-enforce)を同一seed(42)・同一CSV(0805系3ファイル、
  exclude_csvにより自動的に短距離4ファイルは除外)で実行し、ログから診断値を抽出して
  比較表を作る`compare_route_source.py`を新規作成した。
- **計測できる指標の限界を先に明記**: 区間ごとの正解位置(タイムスタンプ付き)を記録した
  データが存在しないため、真のRMSE・曲がり位置誤差は計算できない。今回計測したのは
  (1)全滅回数、(2)有効粒子率・経路内率・Neff平均・位置分散平均(既存の診断ログ)、
  (3)最終推定位置、(4)終点x誤差=|最終x - 800|(ユーザーが実測時の記憶から申告した
  x=800前後という値を根拠とする。目標距離のtarget_distance_px校正とは独立に、結果の
  評価にのみ用いている)。
- **結果(結果CSV: `results/20260815_151053_route_source_comparison.csv`)**:
  条件別平均は none: 全滅回数15.0/経路内率1.000/終点x誤差457.1px、manual-enforce:
  11.7/0.944/268.9px、auto-enforce: 15.7/0.585/345.3px。
  **平均だけを見るとauto-enforceはmanual-enforceより全滅回数・終点誤差ともに悪化しており、
  「自動抽出の方が一律に優れている」とは言えない結果になった。** ただし条件・ファイル別に
  見ると単純ではなく、`pdr_log_0805_1442.csv`(唯一L字を曲がって遠方まで到達するファイル)
  だけはauto-enforceの方が明確に良い(全滅回数3 vs 9、終点誤差58.8px vs 128.6px)。
  一方`pdr_log_0805_1438/1441.csv`(L字手前で終了する2ファイル)はauto-enforceの方が
  明確に悪い(全滅回数24/20 vs 12/14、経路内率0.629/0.418 vs 0.987/0.865)。
- **原因調査**: マスク幅を数値比較したところ(x=100〜800の断面でauto/manualの縦方向画素数
  を比較)、下側の廊下(x=100〜300、1438/1441が主に滞在する区間)ではauto幅=24px、
  manual幅=37pxで、**手動マスクの方が実際の廊下より広い**(route_width_px=18pxの円形
  バッファは半径18pxなので幅36〜37pxになり、実測した現実の廊下幅24〜28pxより広い)。
  一方、右側の廊下(x=500〜800、1442が通過する区間)ではauto幅が47〜104pxと変動し
  manual幅37pxより広い区間が多い(ドア部分のくびれや部屋との接続で通路が実際に広い
  ため)。つまり、**自動抽出マスクは実際の建物形状に忠実な幅を反映するため、狭い区間
  では手動マスクより制約が厳しくなりPFが不安定化し(全滅回数増)、逆に実際に広い区間
  ではより多くの空間的余裕を与えて安定化する**、という一貫した説明ができる。
  手動マスクの一様18px半径バッファは、狭い区間で実際より広い「下駄」を履かせることで
  見かけ上の安定性を生んでいた可能性がある——これは方式D(手動優先)の隠れた有利さであり、
  卒論の考察(8.6節)で正直に書くべき点。
- **結論として伝えたこと**: 「自動抽出はより現実に忠実な制約を与えるが、その分PFの
  真の脆弱性(特に狭い廊下でのオドメトリ誤差蓄積)がそのまま結果に表れる」という
  発見自体は本研究独自の考察として価値がある。ただし現時点でauto-enforceが
  manual-enforceを一律に上回るとは主張できないため、そのまま「性能向上した」とは
  報告していない。
- **今後**: (1) `auto_route_max_half_width_px`の閾値を調整する、または抽出後の
  マスクに数px程度の緩和的膨張を加える、で狭い区間での過剰制約を緩和できるか検証、
  (2) 全滅回数増加の直接原因(オドメトリの一歩あたりの推定誤差)を掘り下げる、
  (3) prefer条件や§6.5(不確実性適応粒子数)も比較表に追加、が候補。

### 2026-08-15: route_source=auto — 二値地図からの経路帯マスク自動抽出を追加

- **背景**: ユーザーから「今のプログラムは2つの参考論文を真似ただけに感じる。オリジナル性を
  高めるには」と相談を受けた。進捗反映版メモ.txt §18.3を確認すると、現状は比較実験でいう
  「方式D：手動経路優先PF」止まりであり、同メモ自身が「正解に近い経路を事前入力するため、
  最終提案方式としてそのまま使用すると公平性に問題がある」と明記していた。最終提案方式
  (方式E)に必要な要素は§6.1〜6.6に列挙済みだが、いずれも未着手だった。
- **優先順位の検討**: §6.5(不確実性に応じた粒子数変更・実装コスト低)、§6.1(通路中心線自動
  抽出・方式Dの弱点に直接効く)、§6.4(複数経路仮説・実装コスト高)を比較した上で、ユーザーが
  §6.1を選択。理由の一つとして、現在の実測CSV(0805系)の経路は分岐の無い単純なL字通路
  であり、複数経路仮説(交差点での分岐)は今のデータでは効果を検証できないという制約もある
  ことを説明済み。
- **設計**: 当初はskeletonize(細線化)+ 順序付きポリライン化(通路グラフ探索)まで検討したが、
  スコープを抑えるため第一段階では「経路帯マスクの空間的制約」のみを自動化する方針にした。
  - `extract_auto_route_mask(binary_for_pf, max_half_width_px)`:
    `dist = distance_transform_edt(free_space)` で壁までの距離(px)を計算し、
    `dist <= max_half_width_px` を満たす移動可能領域を「通路候補」とみなす。広い部屋は
    中心付近の距離値が大きくなるため自然に候補から外れる。候補領域の連結成分のうち
    最大のものを通路網として採用する(`scipy.ndimage.label`)。新規ライブラリ依存なし
    (numpy/scipyのみ)。
  - 事前に`kanri_4f_binary_final3.png`で単体検証: `half_width_px`を16/18/20/22pxで
    振っても、既知route_points(手動確認済みの4点)は常に最大連結成分に属し、次点の
    連結成分よりサイズが約5倍以上大きいことを確認(=閾値に対して結果が安定)。
    可視化(`/private/tmp/.../scratchpad/auto_route_check.png`、セッション終了で消える
    一時ファイル)でも、抽出領域が手動route_pointsの形状をほぼそのままなぞり、
    かつ実際のドア部分のくびれ形状まで反映していることを目視確認した。
  - **座標ゼロ化の方針**: `route_source=auto`の場合、JSONの`route_points`は意図的に
    無視する(`ROUTE_POINTS = []`にする)。中途半端に手動座標を残すと「本当に自動か」
    という主張が弱くなるため、空間マスクは完全に地図由来、という状態を明確にした。
  - **未対応と明記した範囲**: `route_guidance_enabled()`系(経路線分に連動した方位補正、
    曲がり角近傍判定)は順序付きポリラインが前提の実装のため、autoモードでは動作しない
    (route_points=[]なので自動的に無効化される)。今回追加したのは方式Eに必要な要素の
    うち§6.1(通路中心線抽出の簡易版)相当のみで、§6.2(通路グラフ)・§6.4(複数経路仮説)・
    §6.5(不確実性適応粒子数)は引き続き未着手。
- **統合**: `--route-source {manual,auto}`(CLI、デフォルトmanualで既存の挙動を変えない)、
  JSON側にも同名の任意設定キー(`route_source`, `auto_route_max_half_width_px`、共に
  未指定なら`manual`/`20.0`)。`main()`で`ROUTE_SOURCE=="auto"`なら
  `extract_auto_route_mask()`、それ以外は従来通り`build_route_mask()`を呼ぶ二択にした。
  `redraw_all_paths()`でも自動抽出マスクを薄い赤で地図に重ね描き(手動route_pointsの
  マゼンタ破線と対比できるように)、プロットタイトル・PNG自動保存ファイル名にも
  `route_source`を追記(`enforce-auto`のように表示)。
- **検証**: `kanri_4f.json`(既存の`enforce`+`android`+`target_distance_px=815`設定)に
  対して`--route-source manual`と`--route-source auto`を同一シード(42)で実行し比較。
  自動抽出マスクの有効画素数=54110px(全343400px中)。3ファイルとも自動マスク内に
  収まり、`pdr_log_0805_1442.csv`は手動版同様L字を曲がって右方向まで到達(最終位置
  manual: x=671.4,y=107.1 / auto: x=741.2,y=126.2、経路帯の形状差により多少ずれるが
  同じ通路をなぞれている)。`pdr_log_0805_1438/1441.csv`は従来通りL字手前で終了。
  結果PNG: `results/20260815_150107_route-enforce-auto_seed-42.png` /
  `results/20260815_150141_route-enforce-manual_seed-42.png`。
- **今後**: (1) 順序付き通路中心線・通路グラフを抽出し方向補正もauto対応させる、
  (2) `route_source=auto`と`manual`を同一条件で回して定量比較する評価基盤(RMSE等、
  進捗メモ§18.3「未完了」)を作る、(3) §6.5(不確実性適応粒子数)に着手、が候補。
  ユーザーには②(本項目)→評価基盤の順で進めることを提案済み。

### 2026-08-15: pdr_pf_improved.pyの未使用コード削除(2097→2085行)

- **調査方法**: `pyflakes`/`vulture`による静的解析 + 全トップレベル関数・定数の
  出現回数を手動集計するpythonスクリプトで、定義はあるが呼び出し/参照が無い箇所を
  特定。
- **削除したもの**:
  - 未使用import `from dataclasses import dataclass`。
  - `estimate_step_length_px()`(Weinberg法の予備実装、呼び出し箇所0件)と、そこでのみ
    使われていた`WEINBERG_K`定数。`estimate_smartpdr_step_length_px()`(SmartPDR式)が
    実際に使われている方で、こちらは残した。
  - `START_X`/`START_Y`グローバル変数、及びJSON任意設定`"start"`ブロックの読み込み。
    実際の各CSVの開始位置は`start_positions.csv`/地図クリックで決まっており、
    この2変数はPFの挙動に一切影響していなかった。それどころか
    `開始位置: x=250.0, y=230.0`という**実態と異なる固定値をログに出力する**だけの
    有害な残骸だった(未使用なだけでなく紛らわしいので削除価値が高いと判断)。
- **誤検知として残したもの**: `vulture`は`CSVHandler`の`on_created`/`on_modified`/
  `on_moved`を「未使用」と報告するが、これは`watchdog`ライブラリがファイル変更時に
  自動で呼び出すコールバックメソッドであり、削除すればフォルダ監視機能(--no-watch
  無し時のデフォルト動作)が壊れる。静的解析ツールの「未使用」報告は必ず実際の
  呼び出し経路(フレームワークのコールバック等)を確認してから判断すること。
- **確認**: 削除後に`--route-constraint-mode enforce --heading-source android
  --target-distance-px 815`で実行し、削除前と完全に同じ推定結果
  (x=671.4, y=107.1)が出ることを確認。
- CLAUDE.mdの「Reference papers」節にあった`WEINBERG_K`への言及2箇所も削除に
  合わせて修正済み。

### 2026-08-15: 0410/0414×2/0624の4CSVをkanri_4f.jsonの実行対象から除外

- **経緯**: enforce+android+`--target-distance-px`での歩幅校正を試みたところ(下の
  エントリ参照)、`pdr_log_0410_1535.csv`, `pdr_log_0414_0902.csv`,
  `pdr_log_0414_0940.csv`, `pdr_log_0624_14.csv`の4CSVはstart→②の地図距離(440px)に
  対して生の推定距離が86〜127pxしかなく、3.5〜5倍のスケーリングが必要という時点で
  そもそも校正が破綻することが判明。ユーザーへ確認したところ、この4CSVは別の図
  (L字経路用、L.png)で既に使用しているものであり、今回のZ字経路(kanri_4f.json)の
  実行対象には含めなくてよいとのこと。
- **対応**: `map_configs/*.json`に任意設定`exclude_csv`(ファイル名の配列、完全一致)
  を追加し、`redraw_all_paths()`のファイル一覧構築時にフィルタするよう
  `pdr_pf_improved.py`を変更。`map_configs/kanri_4f.json`の`exclude_csv`に上記4CSVを
  設定済み。これにより`kanri_4f.json`を使う実行では常にこの4CSVはスキップされ、
  `pdr_log_0805_*`の3CSVのみが処理される。
- **注意**: この4CSVを使う別の図(L.png)がどのプログラム/設定で生成されているかは
  未確認。もし同じ`pdr_pf_improved.py`を別の`map_configs/*.json`(route_pointsが
  L字用)で実行しているなら、そちらのJSONではこの4CSVを`exclude_csv`に含めない
  こと。

### 2026-08-15: pdr_pf_improved.pyの軌跡がy≈300に張り付く問題の調査とroute_points修正

- **症状**: `--route-constraint-mode none`で実行すると、全CSVの推定軌跡が開始位置付近
  (y≈300近辺)に留まり、実際に歩いたはずの経路を辿らない。結果画像だけを見ると全CSVが
  同じような場所で停滞しているように見えた。
- **原因調査の過程**:
  - `ParticleFilterPDR.is_in_wall()`を一時的に無効化した「壁制約なしの生PDR」で試したと
    ころ、ステップ数の多いCSV(0805系)は方位が大きくドリフトし、地図の外
    (y=600〜700px相当)まで暴走することを確認。
  - 原因はMadgwickフィルタが`updateIMU`(ジャイロ+加速度のみ)で動作しており、
    全CSVに`mag_x/mag_y/mag_z`列が存在しないため磁北による絶対補正が一切効かず
    (`has_magnetometer()`は常にFalseを返す)、ジャイロ単体積分の誤差が時間とともに
    際限なく蓄積するため。
  - 壁制約ありの通常実行では、ドリフトした方位のまま進むとすぐ壁に当たって全滅
    (全粒子重み0)が頻発し(CSVによって最大16回)、全滅時リカバリ処理がほぼ同じ場所へ
    粒子を再配置するため、結果的に開始位置付近で往復しているように見えていた。
    **PF側のバグではなく、入力方位の誤差が主因**と判断。
  - `--heading-source android`(yaw_deg列を使う。0805系のみ保有)に切り替えると南への
    暴走は解消したが、それでも実経路とは一致しなかった。最終原因は
    `map_configs/kanri_4f.json`の`route_points`が実際に歩いた経路と大きくずれていた
    ことだった(次項)。
- **route_points修正の経緯**: ユーザーが地図画像に実際の経路を矢印で書き込んだ画像を
  提供 → Claude Codeが地図をグリッド付きで拡大した画像を生成して候補座標を提示 →
  ユーザーが目視で確認・修正指示 → 曲がり角付近は目視だけでは正確に判断できなかった
  ため、最終的にNumPyで二値地図の画素を直接走査し、目視では壁に見えた場所が実際には
  通行可能かを確定させて合意した。**曲がり角の判定は目視だけに頼らず、画素値を直接
  確認するのが確実**(教訓として記録)。
- **確定した経路**(地図ピクセル座標、`kanri_4f_binary_final3.png`基準):
  - `start(100,230) → ①(425,230) → ②(425,115) → ③(900,115)`
  - `pdr_log_0410_1535.csv`, `pdr_log_0414_0902.csv`, `pdr_log_0414_0940.csv`,
    `pdr_log_0624_14.csv`の4CSVは①②まで(直角ターン1回)で計測終了。
  - `pdr_log_0805_1438.csv`, `pdr_log_0805_1441.csv`, `pdr_log_0805_1442.csv`の3CSVは
    ③まで(直進→曲がり→再度直進のZ字経路)歩いている。
- **`map_configs/kanri_4f.json`へ反映済み**(route_points/route_widthなど)。
- **未解決の注意点**:
  - `route_points`は同JSON内で1本のグローバル設定であり、CSVごとに経路長を変える
    仕組みが無い。①②止まりの4CSVに対して`route_constraint_mode=prefer`/`enforce`を
    使うと、実際には歩いていない③への区間まで経路として与えてしまうため、この4CSVに
    ついては`prefer`/`enforce`の結果を額面通り評価しない(または経路長を分けて実行する
    仕組みを追加する)よう注意すること。
  - 磁気センサ列が無いという制約自体は今回のroute_points修正では解決していない。
    `none`モードでの長時間CSV(特に0805系)は今後も方位ドリフトの影響を受けるため、
    `prefer`/`enforce`モード(今回確定したroute_pointsを使用)か、将来的な磁気センサ
    付き再計測が必要(進捗メモ§4.1/§5.3/§20.2の「手動経路への依存を減らす」課題とも関連)。
- **修正後の動作確認**: `--route-constraint-mode prefer`で実行すると、①②止まりの4CSVは
  開始位置付近に妥当に留まり、③まで歩いた3CSVは修正後のx=425の曲がり角で正しく曲がる
  ようになった(修正前はx=440で曲がろうとして経路とずれていた)。ただし`pdr_log_0805_1438.csv`
  は`--heading-source android`でも上の部屋側へ逸れる傾向が残った — 個別CSVの方位品質の
  問題であり、route_points側の問題ではない。`none`モードは`route_points`を一切使わない
  ため、この修正の効果は`prefer`/`enforce`でのみ確認できる(`none`のままだと見た目は
  修正前と変わらない — この点を今後の実行結果の解釈時に混同しないこと)。

### 2026-08-15: preferモードでもroute_points通りの通路から逸脱する問題(3つ目区間)

- **症状**: route_points修正後、`prefer`モードで`pdr_log_0805_1438.csv`/`_1441.csv`が
  ②(425,115)以降の直線区間(本来の廊下、y≈100-130の帯)から外れ、上の部屋側(y<100)へ
  逸れて歩く。`_1442.csv`は問題なし。
- **原因**: `prefer`は経路外でも重み`off_route_weight`(=0.15)を残すソフトな制約であり、
  `is_in_wall()`は`route_mask`を見ない設計([CLAUDE.md](CLAUDE.md)のScript lineage節
  参照)。y=100の壁には扉の隙間(物理的に通行可能)があり、方位誤差でそこを抜けると
  経路外でも重みが残ったまま部屋側で推定され続けていた。
- **対応**: `route_constraint_mode=enforce`に切り替えると解消することを確認。enforceは
  経路外の重みを完全に0にするため、扉を抜けようとした粒子群はその場で重み0になり、
  既存の修正(全滅時リカバリのroute_mask考慮)により経路内へ引き戻される。実行結果
  (`results/20260815_141147_route-enforce_seed-42.png`)では3ファイルとも②以降
  y=100-130の帯から外れなくなった。
  - `1438`のみ上端(y≈95-100)付近で若干揺れるが、ピクセル値を確認したところ
    その付近(x=470-600)のy=100は実際に薄い壁(2〜3px)でしっかり閉じており、
    部屋側へは越えていない(揺れているだけで漏れてはいない)。
  - 3ファイルとも③(x=900)まで届かずx=470〜560あたりで軌跡が止まる。これは歩幅
    校正が無効("総距離校正: 無効")のまま総移動距離が過小推定されている可能性が高く、
    route_pointsの問題ではない(別課題として残す — `--target-distance-px`による校正、
    または実測距離の確認が必要)。
- **結論**: `0805`系3CSVを経路制約ありで実行する場合は`prefer`ではなく`enforce`を
  使うこと(`prefer`は扉などの物理的な隙間がある区間で経路外へ漏れ得る)。

### 2026-08-15: pdr_pf_improved.pyのpdr_pf_clickstart.pyとの差分解消

CLAUDE.mdの「Script lineage」節で指摘されていた下記4点を修正:

1. `--save`未指定時にPNGが1枚も保存されない問題を修正し、`pdr_pf_clickstart.py`同様
   `results/`ディレクトリへ実行日時・経路制約モード・乱数シードを含む名前で自動保存
   するようにした。
2. `apply_map_config()`が`config.get(key, 旧デフォルト値)`で設定漏れを黙って握り
   潰していた(ヘッダーの「設定漏れはエラー」という記述と矛盾)問題を、
   `require_config_value()`による必須チェックへ統一して解消。
3. `--heading-source android`をyaw_deg列の無いCSVに使った際の`ValueError`が
   `redraw_all_paths()`のループ外まで伝播し、以降のCSVを1枚も処理できずバッチ全体が
   止まっていた問題を、該当ファイルだけスキップするtry/exceptで解消。
4. `[SmartPDR]`/`[先行研究:移動様態PF]`/`[本研究独自]`の出典タグと、研究的位置づけの
   説明コメントを`pdr_pf_clickstart.py`から移植。

また、`route_constraint_mode=enforce`で全粒子の重みが0になった際の自己リカバリ処理に
経路マスク条件を追加(全滅からの回復時に粒子群が経路コリドー外へ漏れ出す問題を修正)。
`is_in_wall()`自体は経路を見ない設計のまま据え置き(意図的な選択 — 理由は上記の
「route_points修正」エントリの前段、及びCLAUDE.mdのScript lineage節の該当箇所を参照)。
